import type { Outlet } from "./outlet";

// The detailed response type is undetermined and will be updated when supplied.
// Typically, it is an array of Outlet objects or a paginated wrapper.
export type ListOutletsResponse = Outlet[];
